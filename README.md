D66-9 Snap Route To Road

D66-8ベース。
今回触ったのはマイルート作成中の「道に合わせる」機能だけ。
- ルート作成カードに「道に合わせる」ボタン追加
- タップした点列をOSRM route APIで道路沿いのGeoJSONルートへ変換
- bike → foot → driving の順にフォールバック
- 補正後の距離を更新
- 保存すると補正後ルートを保存
- 点が多すぎる場合は最大25点に間引いて送信
- 既存の地図ビュー、下バー、建物/地形3D、SVなし状態は維持
- JS構文チェック済み

D66-10 Edit Route Points

D66-9ベース。
今回触ったのはマイルート作成後のポイント編集だけ。
- ルート作成中の点をドラッグで移動
- 点をタップして選択
- 「選択点削除」で選んだ点を削除
- 「1つ戻す」は最後の点を削除
- 「道に合わせる」後も、元の操作点だけを残して編集可能
- 点を移動/削除したら直線状態に戻し、再度「道に合わせる」で補正
- 保存時は補正済みルートを保存、操作点もwaypointsとして保持
- 既存の地図ビュー、下バー、建物/地形3D、SVなし状態は維持
- JS構文チェック済み
